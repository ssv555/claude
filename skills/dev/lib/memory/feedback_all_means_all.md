---
name: «ВСЕ» / «ALL» — буквально весь скоуп, без моей фильтрации
description: Когда пользователь капсом просит «ВСЕ»/«ALL»/«ПОЛНЫЙ» — перечислять весь известный скоуп без отбора по релевантности
type: feedback
originSessionId: 5394acb7-72c0-4ef7-a739-11ec2270b2ac
---
Когда пользователь пишет капсом «ВСЕ объекты» / «ALL» / «ПОЛНЫЙ список» — перечисляю весь известный скоуп (все 19 таблиц, все 6 enum-ов, все routes, все компоненты), а не только те, что я считаю «затронутыми задачей».

**Why:** Инцидент 2026-05-05 — пользователь два раза просил «все объекты для дропа» в контексте MVP fresh install. Я давал только переименованные таблицы и enum-ы (4+2), потому что мысленно фильтровал по «затронуты ли rename-ом». Пользователь хотел ВЕСЬ список (для полной очистки БД на MVP). После третьего капс-крика дал полный (19+6).

**How to apply:**
- Если в запросе есть КАПС-маркер «ВСЕ» / «ALL» / «ПОЛНЫЙ» / «ВЕСЬ» / «ВЕСЬ скоуп» — выключить внутренний фильтр «релевантности».
- Перечислить весь известный скоуп. Если есть сомнение, что мой список полный — добавить «если что-то пропустил — скажи».
- Не интерпретировать «все» как «все, которые я считаю относящимися к задаче». Контекст обычно шире, чем кажется.
- Применяется к: спискам таблиц, enum-ов, файлов, routes, компонентов, переменных env, тестов.
