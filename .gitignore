# Credentials & secrets
.credentials.json
.secrets/

# Scratch
tmp/

# Caches
cache/
stats-cache.json
mcp-needs-auth-cache.json

sessions/

# Auto-generated runtime data
projects/
file-history/
plugins/
shell-snapshots/
debug/
todos/
plans/
backups/
downloads/
commands/
scripts/
ide/
history.jsonl

# Telemetry
statsig/
telemetry/

# Old skills .git (if leftover)
skills/.git/

# Skill dependencies & scratch — regenerated, never commit
node_modules/
skills/**/.temp-execution-*.js

# Dev archives (server-side dumps, too large for git — keep on local disk only)
developers/*/_archive/
